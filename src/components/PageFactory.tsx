import React from 'react';
import { NavLink } from 'react-router-dom';
import '../styles/navbar.css';

class PageFactory extends React.Component {
    constructor(props: Array<[string, string]>) {
        super(props);
    
    }
    render() {
        return(<></>);
    };
}
export default PageFactory;